#!/bin/bash -x
RepoName=$1
git config --global user.name "mtle"
git config --global user.email "jst.mle@gmail.com"
git config --global credential.helper cache
git init
git add hw lab
#git remote add origin $RepoName
#git pull origin master
#git push -u origin master
